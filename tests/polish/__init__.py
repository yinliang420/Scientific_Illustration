"""Polish test package for huitu v0.6 (4-agent test-review-fix iteration).

Each ``test_NN_*.py`` covers one user-reported concern:
  * test_01 — font consistency across journal presets
  * test_02 — SVG editability (savefig roundtrip)
  * test_03 — semantic palette consistency
  * test_04 — layout precision (overlaps, clips, alignment)
"""
